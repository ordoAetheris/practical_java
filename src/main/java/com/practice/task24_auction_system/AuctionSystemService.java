package com.practice.task24_auction_system;

import java.time.LocalDateTime;
import java.util.*;

/**
 * СИСТЕМА АУКЦИОНОВ
 *
 * <p>Реализуйте систему онлайн-аукциона. Продавцы выставляют лоты,
 * покупатели делают ставки, аукцион завершается и определяется победитель.</p>
 *
 * <p>Сущности:</p>
 * <ul>
 *   <li>Lot — лот (id, title, description, startPrice, currentPrice, sellerId, status ACTIVE/SOLD/EXPIRED)</li>
 *   <li>Bid — ставка (id, lotId, bidderId, amount, timestamp)</li>
 * </ul>
 *
 * <p>Операции:</p>
 * <ul>
 *   <li>Создать лот с начальной ценой</li>
 *   <li>Сделать ставку на лот</li>
 *   <li>Завершить аукцион по лоту (определить победителя)</li>
 *   <li>Получить список активных лотов</li>
 *   <li>Получить историю ставок по лоту</li>
 *   <li>Получить лоты пользователя (как продавца)</li>
 * </ul>
 *
 * <p>Бизнес-правила:</p>
 * <ul>
 *   <li>Ставка должна быть строго больше текущей цены лота</li>
 *   <li>Продавец не может делать ставки на свой собственный лот</li>
 *   <li>Нельзя делать ставку на завершённый или истёкший лот</li>
 *   <li>Начальная цена должна быть положительной</li>
 * </ul>
 *
 * <p>Уровень: Базовый</p>
 *
 * <h3>Запуск из терминала:</h3>
 * <pre>
 * javac src/main/java/com/practice/task24_auction_system/AuctionSystemService.java
 * java -cp src/main/java com.practice.task24_auction_system.AuctionSystemService
 * </pre>
 *
 * <p><b>УСЛОЖНЕНИЯ (сверх базы — полные условия в блоке внизу файла):</b></p>
 * <ul>
 *   <li><b>1. Атомарное обновление текущей цены</b> — конкурентные ставки не теряются (lost update).</li>
 *   <li><b>2. Граница закрытия аукциона</b> — ставка в момент finish либо принята, либо отклонена согласованно.</li>
 *   <li><b>3. Anti-sniping</b> — ставка в последние секунды продлевает срок аукциона.</li>
 * </ul>
 */
public class AuctionSystemService {

    public enum LotStatus {
        ACTIVE, SOLD, EXPIRED
    }

    public record Lot(
            String id,
            String title,
            String description,
            double startPrice,
            double currentPrice,
            String sellerId,
            LotStatus status
    ) {}

    public record Bid(
            String id,
            String lotId,
            String bidderId,
            double amount,
            LocalDateTime timestamp
    ) {}

    /**
     * Создаёт новый лот на аукционе.
     *
     * @param title       название лота
     * @param description описание лота
     * @param startPrice  начальная цена
     * @param sellerId    идентификатор продавца
     * @return созданный лот со статусом ACTIVE
     * @throws IllegalArgumentException если startPrice <= 0 или title пустой
     */
    public Lot createLot(String title, String description, double startPrice, String sellerId) {
        // TODO: implement
        return null;
    }

    /**
     * Делает ставку на лот.
     *
     * @param lotId    идентификатор лота
     * @param bidderId идентификатор участника
     * @param amount   сумма ставки
     * @return сделанная ставка
     * @throws IllegalArgumentException если лот не найден или ставка <= текущей цены
     * @throws IllegalStateException    если лот не активен или bidderId == sellerId
     */
    public Bid placeBid(String lotId, String bidderId, double amount) {
        // TODO: implement
        return null;
    }

    /**
     * Завершает аукцион по лоту. Если были ставки — статус SOLD, иначе EXPIRED.
     *
     * @param lotId идентификатор лота
     * @return обновлённый лот
     * @throws IllegalStateException если лот уже завершён
     */
    public Lot finishAuction(String lotId) {
        // TODO: implement
        return null;
    }

    /**
     * Возвращает список всех активных лотов.
     *
     * @return список активных лотов
     */
    public List<Lot> getActiveLots() {
        // TODO: implement
        return List.of();
    }

    /**
     * Возвращает историю ставок по лоту, отсортированную по времени (новые первыми).
     *
     * @param lotId идентификатор лота
     * @return список ставок
     */
    public List<Bid> getBidHistory(String lotId) {
        // TODO: implement
        return List.of();
    }

    /**
     * Возвращает все лоты указанного продавца.
     *
     * @param sellerId идентификатор продавца
     * @return список лотов продавца
     */
    public List<Lot> getLotsBySeller(String sellerId) {
        // TODO: implement
        return List.of();
    }

    public static void main(String[] args) {
        System.out.println("=== AuctionSystemService: Smoke Test ===");
        // TODO: создать тестовые данные и проверить основные сценарии
    }
}

/* ═══════════════════════ УСЛОЖНЕНИЯ (реализовать ИЗНУТРИ этого класса) ═══════════════════════
 *
 * Три НЕЗАВИСИМЫХ усложнения. Здесь — ЗАДАЧА, КОНТРАКТ и в чём ловушка. Способ решения и тесты — сам.
 *
 * ─── 1. АТОМАРНОЕ ОБНОВЛЕНИЕ ТЕКУЩЕЙ ЦЕНЫ (максимальной ставки) ───
 *   Бизнес: ставка принимается, только если она строго больше текущей цены лота; принятая ставка
 *     становится новой текущей ценой. Под конкуренцией ни одна валидная ставка не должна теряться.
 *   Контракт: placeBid принимает ставку и обновляет текущую цену как единое целое; ставки меньше или
 *     равные текущей отклоняются. Инвариант: текущая цена лота равна максимальной принятой ставке.
 *   Ловушка: наивная «прочитал текущую цену → сравнил → записал свою» под несколькими потоками теряет
 *     ставки — двое прочитали цену 100, оба поставили 110, одна затёрла другую (lost update).
 *   Проверить: N растущих ставок параллельно → текущая цена равна реальному максимуму; меньшие и равные
 *     отклонены.
 *
 * ─── 2. ГРАНИЦА ЗАКРЫТИЯ АУКЦИОНА (ставка против завершения) ───
 *   Бизнес: ставки, сделанные до закрытия, учитываются; сделанные после — нет. Граница должна быть
 *     чёткой и согласованной.
 *   Контракт: при одновременных placeBid и finishAuction ставка либо попадает в аукцион до закрытия,
 *     либо отклоняется как поздняя — без состояния «лот закрыт, но ставка ещё сядет». Инвариант:
 *     победитель определяется строго по ставкам, принятым до закрытия.
 *   Ловушка: ставка приходит ровно в момент finishAuction — гонка «ставлю» против «закрываю».
 *   Проверить: победитель определён по ставкам строго до закрытия; ставки после закрытия отклонены.
 *
 * ─── 3. ANTI-SNIPING (продление при ставке в последние секунды) ───
 *   Бизнес: чтобы нельзя было выиграть ставкой в самую последнюю секунду, ставка в последние T секунд
 *     продлевает срок аукциона ещё на T. Аукцион закрывается, только когда после последней ставки
 *     прошло «тихое» окно без новых ставок.
 *   Контракт: placeBid в пределах последних T секунд до дедлайна сдвигает дедлайн; приём ставки и
 *     продление дедлайна согласованы между собой под конкуренцией.
 *   Ловушка: приём ставки и продление дедлайна — две связанные операции; под конкуренцией они могут
 *     рассинхронизироваться (ставка принята, а дедлайн не продлён, или наоборот).
 *   Проверить: серия поздних ставок отодвигает дедлайн; закрытие происходит только после «тихого» окна.
 */
