package com.practice.task26_review_service;

import java.time.LocalDateTime;
import java.util.*;

/**
 * СЕРВИС ОТЗЫВОВ
 *
 * <p>Реализуйте сервис управления отзывами на товары. Пользователи оставляют отзывы
 * с рейтингом, отзывы проходят модерацию, на основе рейтингов формируется топ продуктов.</p>
 *
 * <p>Сущности:</p>
 * <ul>
 *   <li>Review — отзыв (id, productId, userId, rating 1-5, text, createdAt, status ACTIVE/MODERATED/DELETED)</li>
 * </ul>
 *
 * <p>Операции:</p>
 * <ul>
 *   <li>Оставить отзыв на продукт</li>
 *   <li>Удалить отзыв (мягкое удаление — перевод в статус DELETED)</li>
 *   <li>Отправить на модерацию</li>
 *   <li>Средний рейтинг продукта (только ACTIVE отзывы)</li>
 *   <li>Отзывы по продукту с сортировкой (по дате или рейтингу)</li>
 *   <li>Отзывы пользователя</li>
 *   <li>Топ-N продуктов по среднему рейтингу</li>
 * </ul>
 *
 * <p>Бизнес-правила:</p>
 * <ul>
 *   <li>Один пользователь может оставить только один отзыв на продукт</li>
 *   <li>Рейтинг строго от 1 до 5 (целое число)</li>
 *   <li>Текст отзыва не может быть пустым</li>
 *   <li>В расчёт среднего рейтинга идут только ACTIVE отзывы</li>
 * </ul>
 *
 * <p>Уровень: Базовый</p>
 *
 * <h3>Запуск из терминала:</h3>
 * <pre>
 * javac src/main/java/com/practice/task26_review_service/ReviewService.java
 * java -cp src/main/java com.practice.task26_review_service.ReviewService
 * </pre>
 *
 * <p><b>УСЛОЖНЕНИЯ (сверх базы — полные условия в блоке внизу файла):</b></p>
 * <ul>
 *   <li><b>1. Один отзыв на пользователя</b> — защита от двух отзывов одного пользователя при конкуренции.</li>
 *   <li><b>2. Согласованный средний рейтинг</b> — параллельные отзывы не рвут подсчёт среднего.</li>
 *   <li><b>3. Идемпотентный отзыв</b> — повтор обновляет существующий, а не создаёт второй.</li>
 *   <li><b>4. Параллельность разных товаров</b> — отзывы на разные товары не мешают друг другу.</li>
 * </ul>
 */
public class ReviewService {

    public enum ReviewStatus {
        ACTIVE, MODERATED, DELETED
    }

    public enum SortBy {
        DATE_ASC, DATE_DESC, RATING_ASC, RATING_DESC
    }

    public record Review(
            String id,
            String productId,
            String userId,
            int rating,
            String text,
            LocalDateTime createdAt,
            ReviewStatus status
    ) {}

    public record ProductRating(
            String productId,
            double averageRating,
            int reviewCount
    ) {}

    /**
     * Оставляет отзыв на продукт.
     *
     * @param productId идентификатор продукта
     * @param userId    идентификатор пользователя
     * @param rating    рейтинг (1-5)
     * @param text      текст отзыва
     * @return созданный отзыв
     * @throws IllegalArgumentException если rating не в диапазоне 1-5 или text пустой
     * @throws IllegalStateException    если пользователь уже оставил отзыв на этот продукт
     */
    public Review addReview(String productId, String userId, int rating, String text) {
        // TODO: implement
        return null;
    }

    /**
     * Мягкое удаление отзыва (перевод в статус DELETED).
     *
     * @param reviewId идентификатор отзыва
     * @throws IllegalArgumentException если отзыв не найден
     */
    public void deleteReview(String reviewId) {
        // TODO: implement
    }

    /**
     * Отправляет отзыв на модерацию.
     *
     * @param reviewId идентификатор отзыва
     * @throws IllegalArgumentException если отзыв не найден
     */
    public void moderate(String reviewId) {
        // TODO: implement
    }

    /**
     * Возвращает средний рейтинг продукта (только ACTIVE отзывы).
     *
     * @param productId идентификатор продукта
     * @return средний рейтинг или 0.0 если отзывов нет
     */
    public double getAverageRating(String productId) {
        // TODO: implement
        return 0.0;
    }

    /**
     * Возвращает отзывы по продукту с указанной сортировкой (только ACTIVE).
     *
     * @param productId идентификатор продукта
     * @param sortBy    критерий сортировки
     * @return отсортированный список отзывов
     */
    public List<Review> getProductReviews(String productId, SortBy sortBy) {
        // TODO: implement
        return List.of();
    }

    /**
     * Возвращает все отзывы пользователя (все статусы).
     *
     * @param userId идентификатор пользователя
     * @return список отзывов пользователя
     */
    public List<Review> getUserReviews(String userId) {
        // TODO: implement
        return List.of();
    }

    /**
     * Возвращает топ-N продуктов по среднему рейтингу (только ACTIVE отзывы).
     *
     * @param n количество продуктов в топе
     * @return список ProductRating, отсортированный по убыванию рейтинга
     */
    public List<ProductRating> getTopProducts(int n) {
        // TODO: implement
        return List.of();
    }

    public static void main(String[] args) {
        System.out.println("=== ReviewService: Smoke Test ===");
        // TODO: создать тестовые данные и проверить основные сценарии
    }
}

/* ═══════════════════════ УСЛОЖНЕНИЯ (реализовать ИЗНУТРИ этого класса) ═══════════════════════
 *
 * Четыре НЕЗАВИСИМЫХ усложнения. Здесь — ЗАДАЧА, КОНТРАКТ и в чём ловушка. Способ решения и тесты — сам.
 *
 * ─── 1. ОДИН ОТЗЫВ НА ПОЛЬЗОВАТЕЛЯ ───
 *   Бизнес: один пользователь может оставить на товар только один отзыв.
 *   Контракт: addReview создаёт отзыв, только если этот userId ещё не оставлял отзыв на данный товар;
 *     иначе отказ. Инвариант: на пару (товар, пользователь) — не более одного отзыва.
 *   Ловушка: один userId оставляет отзыв из двух потоков одновременно — наивная «проверил, что отзыва
 *     нет → добавил» создаст два (TOCTOU).
 *   Проверить: несколько потоков одним userId → создан ровно один отзыв.
 *
 * ─── 2. СОГЛАСОВАННЫЙ СРЕДНИЙ РЕЙТИНГ ───
 *   Бизнес: средний рейтинг товара должен быть точным при массовом добавлении и удалении отзывов.
 *   Контракт: getAverageRating под конкурентными addReview / deleteReview не даёт рваного результата;
 *     сумма оценок и число отзывов остаются согласованными. Инвариант: среднее = сумма ACTIVE-оценок,
 *     делённая на число ACTIVE-отзывов, без потерь.
 *   Ловушка: наивное обновление суммы и счётчика под конкуренцией теряет часть отзывов (lost update),
 *     и среднее «плывёт».
 *   Проверить: K отзывов с известными оценками → среднее точное; после удалений среднее пересчитано верно.
 *
 * ─── 3. ИДЕМПОТЕНТНЫЙ ОТЗЫВ ───
 *   Бизнес: повтор отзыва тем же пользователем на тот же товар — это правка существующего отзыва, а не
 *     второй отзыв.
 *   Контракт: повторный addReview для пары (userId, productId) обновляет существующий отзыв (в том числе
 *     пересчитывает средний рейтинг со старой оценки на новую), а не создаёт второй.
 *   Ловушка: два запроса на отзыв от одного пользователя на один товар приходят одновременно.
 *   Проверить: повтор (userId, productId) → отзыв один, средний рейтинг учитывает новую оценку вместо старой.
 *
 * ─── 4. ПАРАЛЛЕЛЬНОСТЬ РАЗНЫХ ТОВАРОВ ───
 *   Бизнес: отзывы на РАЗНЫЕ товары не должны мешать друг другу — их средние рейтинги независимы.
 *   Контракт: операции над отзывами разных товаров не сериализуются между собой.
 *   Ловушка: один общий замок на весь сервис корректен, но все товары встают в одну очередь.
 *   Проверить: отзывы на разные товары идут параллельно; на один товар — согласованно.
 */
